# Yarn resolutions showcase

Changing the `debug` dependency of a dependency to our own `debug-noop` package resulting in no debug messages.

|Before adding resolutions|After|
|-|-|
|![Before](before.png)|![After](after.png)|
